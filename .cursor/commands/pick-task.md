---
description: Pick an open hackathon task from TASKS.md and claim it
---

Read `TASKS.md` and `AGENTS.md`.

Show the user the open, unclaimed tasks (title, **ID**, priority). If they already named an ID in this chat, use that.

Then claim the chosen task: add `(@current-branch)` on the title, set **Status** to `claimed`, commit and push immediately, then implement that task only (acceptance criteria in TASKS.md). Do not start a second task in this run.
